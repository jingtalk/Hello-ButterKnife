<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_fragmentStatic"
            android:icon="@drawable/ic_menu_camera"
            android:title="fragmentStatic" />
        <item
            android:id="@+id/nav_fragmentDynamic"
            android:icon="@drawable/ic_menu_slideshow"
            android:title="fragmentDynamic" />
        <item
            android:id="@+id/nav_listActivity"
            android:icon="@drawable/ic_menu_manage"
            android:title="listActivity" />
        <item
            android:id="@+id/nav_listView"
            android:icon="@drawable/ic_menu_gallery"
            android:title="listView" />
    </group>

    <item android:title="Communicate">
        <menu>
            <item
                android:id="@+id/nav_share"
                android:icon="@drawable/ic_menu_share"
                android:title="Share" />
            <item
                android:id="@+id/nav_send"
                android:icon="@drawable/ic_menu_send"
                android:title="Send" />
        </menu>
    </item>

</menu>
